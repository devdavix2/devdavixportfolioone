import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Obi Chinonso – DevDavix | Graphic Design & Full Stack Development",
  description:
    "Portfolio of Obi Chinonso (DevDavix), showcasing expertise in Graphic Design and Full Stack Development.",
  keywords: ["graphic design", "full stack development", "web development", "portfolio", "DevDavix", "Obi Chinonso"],
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}


import './globals.css'